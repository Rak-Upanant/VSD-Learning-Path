# Reference materials — kept local only.
# Contains third-party (ABB) copyrighted documents that must NOT be published.
Resource/

# OS / editor cruft
Thumbs.db
.DS_Store
.vscode/

# Local agent/editor tooling (preview config, local settings) — not for publishing
.claude/
